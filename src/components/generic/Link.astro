---
interface Props {
  where: string;
  what: string;
  target?: string;
}

const { where, target = "_blank", what } = Astro.props;
---

<a
  href={where}
  target={target}
  class="hover:color-white transition-colors"
  >{what}</a
>
