---
interface Props {
  where: string;
  what: string;
  target?: "_self" | "_blank";
}

const { where, target = "_self", what } = Astro.props;
---

<a
  href={where}
  target={target}
  class="hover:color-white transition-colors"
  >{what}</a
>
